import { Interpolation } from 'emotion';
import React from 'react';
import { Theme } from '../../styles';
export interface MonthViewProps {
    visibleDate?: Date;
    onDayClick?(day: Date): void;
    onDayHover?(day: Date): void;
    renderDay?(day: Date): React.ReactNode;
    renderWeekName?(firstWeekDay: Date): React.ReactNode;
    createDayStyles?(day: Date, props: MonthViewProps): Interpolation;
}
export declare function MonthView(props: MonthViewProps): JSX.Element;
export declare namespace MonthView {
    var defaultProps: Partial<MonthViewProps>;
}
export declare const createStyles: (theme: Theme) => {
    table: React.CSSProperties;
    day: React.CSSProperties;
    active: React.CSSProperties;
};
