import React from 'react';
import { ExternalStyles, Theme } from '../../styles';
import { Omit } from '../../util';
export interface ModalBackdropProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'style'> {
    style?: ExternalStyles;
}
export declare function ModalBackdrop(props: ModalBackdropProps): JSX.Element;
export declare const styles: (theme: Theme) => {
    backdrop: React.CSSProperties;
};
