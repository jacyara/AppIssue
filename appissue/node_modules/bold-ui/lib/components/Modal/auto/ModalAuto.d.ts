import React from 'react';
import { ButtonProps } from '../../Button';
import { ModalProps } from '../Modal';
export declare type ButtonAction = ButtonProps & {
    label?: React.ReactNode;
};
export interface ModalAutoProps {
    actions?: ButtonAction[];
    size?: ModalProps['size'];
    render(renderProps: ModalAutoRenderProps): React.ReactNode;
    dispose(): void;
    onClose?(): any;
}
export interface ModalAutoState {
    open: boolean;
}
export interface ModalAutoRenderProps {
    close(): void;
}
export declare const ModalAuto: React.MemoExoticComponent<(props: ModalAutoProps) => JSX.Element>;
