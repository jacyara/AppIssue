import React from 'react';
import { ModalContainerProps } from './ModalContainer';
export declare type ModalSize = 'small' | 'large' | 'auto';
export interface ModalProps extends ModalContainerProps {
    open: boolean;
    size?: ModalSize;
    children?: React.ReactNode;
    /**
     * Specify whether the `onClose` prop should called when backdrop is clicked.
     * @default true
     */
    closeOnBackdropClick?: boolean;
}
export declare function Modal(props: ModalProps): JSX.Element;
export declare namespace Modal {
    var defaultProps: Partial<ModalProps>;
}
