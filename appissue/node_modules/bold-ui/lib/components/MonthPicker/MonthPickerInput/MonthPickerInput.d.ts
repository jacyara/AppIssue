import React from 'react';
import { Omit } from '../../../util';
import { MaskedTextFieldProps } from '../../MaskedTextField';
import { ReferenceMonth } from '../MonthPicker/MonthPicker';
export interface MonthPickerInputProps extends Omit<MaskedTextFieldProps, 'value' | 'onChange'> {
    value?: ReferenceMonth;
    onChange?(referenceMonth: ReferenceMonth): void;
}
export declare class MonthPickerInput extends React.PureComponent<MonthPickerInputProps> {
    render(): JSX.Element;
    private renderInput;
    private onValueChange;
    private onInputChange;
}
export declare type MonthInputProps = Omit<MaskedTextFieldProps, 'mask'>;
export declare const format: (value: ReferenceMonth) => string;
export declare const isValidInput: (value: string) => boolean;
