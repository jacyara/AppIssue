import { Theme } from '../../styles';
import { CalendarProps } from '../Calendar';
export interface CalendarPopupProps extends CalendarProps {
}
export declare function CalendarPopup(props: CalendarPopupProps): JSX.Element;
export declare const createStyles: (theme: Theme) => {
    root: {
        background: string;
        boxShadow: string;
        borderRadius: number;
        padding: string;
        outline: string;
    };
};
