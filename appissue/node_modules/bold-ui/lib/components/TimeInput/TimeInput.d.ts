import { Omit } from '../../util';
import { MaskedTextFieldProps } from '../MaskedTextField';
export interface TimeInputProps extends Omit<MaskedTextFieldProps, 'onChange'> {
    /**
     * Original input change event handler
     */
    onInputChange?: MaskedTextFieldProps['onChange'];
    onChange?(value: string): any;
}
export declare function TimeInput(props: TimeInputProps): JSX.Element;
export declare const padTime: (value: string) => string;
