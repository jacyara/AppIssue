import React from 'react';
import { Theme } from '../../styles';
import { PopperFocusProps } from '../Popper/PopperFocus';
export interface PopoverProps {
    text: string;
    title?: string;
    placement?: PopperFocusProps['placement'];
    children?: React.ReactNode;
}
export declare function Popover(props: PopoverProps): JSX.Element;
export declare namespace Popover {
    var defaultProps: Partial<PopoverProps>;
}
export interface PopoverBaseProps {
    title?: string;
    children?: React.ReactNode;
}
export declare function PopoverBase(props: PopoverBaseProps): JSX.Element;
export declare const createStyles: (theme: Theme) => {
    base: React.CSSProperties;
    title: React.CSSProperties;
};
