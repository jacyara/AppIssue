import React from 'react';
import { DropzoneOptions } from 'react-dropzone';
import { Theme, WithStylesProps } from '../../styles';
export interface FileUploaderProps extends DropzoneOptions {
    file?: FileProps;
    text?: string;
}
export interface FileProps {
    error?: boolean;
    progress?: number;
    selectedFile?: File;
    uploading?: boolean;
}
export declare function FileUploader(props: FileUploaderProps): JSX.Element;
export interface FileDetailsProps extends WithStylesProps {
    file: FileProps;
}
export declare function FileDetails(props: FileDetailsProps): JSX.Element;
export interface FileInfoProps {
    file: FileProps;
}
export declare function FileInfo(props: FileInfoProps): JSX.Element;
export interface FileExtensionProps {
    extension: string;
}
export declare function FileExtension(props: FileExtensionProps): JSX.Element;
export declare const createStyles: (theme: Theme) => {
    dropzone: React.CSSProperties;
    wrapper: React.CSSProperties;
    dragActive: {
        borderColor: string;
    };
    file: {
        borderTop: string;
        padding: string;
    };
    fileDetailsContainer: {
        alignItems: string;
        borderTop: string;
        display: string;
        padding: string;
    };
    fileDetails: {
        flexGrow: number;
        marginLeft: string;
    };
    fileInfo: {
        alignItems: string;
        display: string;
    };
};
