import React from 'react';
import { ExternalStyles, Theme } from '../../styles';
import { AlignItems, JustifyContent } from '../Grid/Grid';
export interface FlowProps {
    direction?: 'horizontal' | 'vertical';
    vSpacing?: number;
    hSpacing?: number;
    alignItems?: AlignItems;
    justifyContent?: JustifyContent;
    style?: ExternalStyles;
    children?: React.ReactNode;
}
export declare function Flow(props: FlowProps): JSX.Element;
export declare namespace Flow {
    var defaultProps: Partial<FlowProps>;
}
export declare const createStyles: (theme: Theme, { alignItems, justifyContent, direction, vSpacing, hSpacing }: FlowProps) => {
    flow: React.CSSProperties;
    flowHorizontal: React.CSSProperties;
    flowVertical: React.CSSProperties;
    child: React.CSSProperties;
};
