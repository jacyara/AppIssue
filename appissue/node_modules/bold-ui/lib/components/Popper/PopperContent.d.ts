import React from 'react';
import { PopperProps } from 'react-popper';
import { Theme } from '../../styles';
import { Omit } from '../../util/types';
export interface PopperContentProps extends Omit<PopperProps, 'children'> {
    show: boolean;
    offset?: number;
    children?: React.ReactNode;
}
export declare function PopperContent(props: PopperContentProps): JSX.Element;
export declare namespace PopperContent {
    var defaultProps: Partial<PopperContentProps>;
}
export declare const createStyles: (theme: Theme, { offset }: PopperContentProps) => {
    content: React.CSSProperties;
    visible: React.CSSProperties;
    hidden: React.CSSProperties;
};
