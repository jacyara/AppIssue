import React from 'react';
import { PopperProps } from 'react-popper';
import { PopperContentProps } from './PopperContent';
export interface PopperFocusProps {
    placement?: PopperProps['placement'];
    offset?: PopperContentProps['offset'];
    children?: React.ReactNode;
    renderPopper?(): React.ReactNode;
}
/**
 * Creates a popper element when the children is hovered or focused.
 */
export declare function PopperFocus(props: PopperFocusProps): JSX.Element;
export declare namespace PopperFocus {
    var defaultProps: Partial<PopperFocusProps>;
}
