import React from 'react';
import { ExternalStyles, Theme, WithStylesProps } from '../../../styles';
export interface TableFooterProps extends WithStylesProps {
    page: number;
    totalPages: number;
    totalElements: number;
    pageSize: number;
    style?: ExternalStyles;
    sizeOptions?: number[];
    onPageChange(page: number): void;
    onSizeChange(size: number): void;
}
export declare function TableFooter(props: TableFooterProps): JSX.Element;
export declare namespace TableFooter {
    var defaultProps: Partial<TableFooterProps>;
}
export declare const createStyles: (theme: Theme) => {
    footer: React.CSSProperties;
    results: React.CSSProperties;
    pagination: React.CSSProperties;
};
